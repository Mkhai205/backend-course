services:
    database:
        image: postgres:15-alpine
        container_name: postgres
        restart: always
        env_file:
            - .env
        ports:
            - "5555:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data

volumes:
    pgdata:
